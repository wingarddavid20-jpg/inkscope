{
  "name": "liquidity-subgraph",
  "version": "1.0.0",
  "private": true,
  "description": "Inkscope liquidity indexer: BridgeTransfer / CexTransfer flows on Ink (Across SpokePool + CEX wallets).",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
